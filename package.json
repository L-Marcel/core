{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build --no-daemon",
    "set:changes": "changeset",
    "set:versions": "changeset version",
    "set:publish": "changeset publish"
  },
  "devDependencies": {
    "changeset": "^0.2.6",
    "tsup": "^6.5.0",
    "turbo": "^1.7.0"
  }
}
